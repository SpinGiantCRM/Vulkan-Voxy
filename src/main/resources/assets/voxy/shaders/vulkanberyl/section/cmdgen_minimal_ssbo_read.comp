#version 460

layout(local_size_x = 1) in;

layout(binding = 0, std430) buffer ProbeBuffer {
    uint probeWord;
};

void main() {
    uint value = probeWord;
    if (value == 0xffffffffu) {
        probeWord = 0u;
    }
}
