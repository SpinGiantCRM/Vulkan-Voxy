#version 460

layout(local_size_x = 128) in;

layout(binding = 2, std430) readonly volatile buffer Binding2ProbeBuffer {
    uint binding2ProbeWord;
};

void main() {
    if (gl_GlobalInvocationID.x != 0u) return;
    uint value = binding2ProbeWord;
    if (value == 0xffffffffu) memoryBarrierBuffer();
}
