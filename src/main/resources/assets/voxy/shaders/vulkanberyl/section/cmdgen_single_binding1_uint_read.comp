#version 460

layout(local_size_x = 1) in;

layout(binding = 1, std430) readonly volatile buffer MetadataUintBuffer {
    uint metadataWord;
};

void main() {
    uint value = metadataWord;
    if (value == 0xffffffffu) {
        memoryBarrierBuffer();
    }
}
