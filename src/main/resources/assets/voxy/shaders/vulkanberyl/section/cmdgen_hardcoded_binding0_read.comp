#version 460

layout(local_size_x = 1) in;

layout(binding = 0, std430) buffer RenderListBuffer {
    uint visibleCount;
    uint indirectLookup[];
};

void main() {
    uint binding0Smoke = visibleCount;
    if (binding0Smoke == 0xffffffffu) {
        visibleCount = indirectLookup[0];
    }
}
